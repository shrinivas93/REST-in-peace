<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.shri</groupId>
	<artifactId>rest-in-peace-spring-boot-starter</artifactId>
	<version>0.1.0-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>rest-in-peace-spring-boot-starter</name>
	<description>
		Spring Boot auto-configuration for REST-in-peace: auto-registers every
		@RestClient interface found on the classpath as a Spring bean, instead
		of one hand-written @Bean method per interface. See
		docs/design/spring-boot-starter.md in the parent repository for the
		full design and rollout plan. A standalone Maven project, not a
		reactor module of the core rest-in-peace library's own pom.xml - it
		depends on that library as an ordinary Maven dependency, the same way
		samples/compile-time-proxy-consumer does. Requires Java 17+ and
		Spring Boot 4.x; the core rest-in-peace library itself stays on Java 8
		and has no dependency on this module either direction.
	</description>

	<!-- Published, versioned, and released independently of the core
	     rest-in-peace artifact (own coordinates, own version, own release
	     workflow), from the same monorepo - see the design doc's
	     "Publishing" section for the full rationale. -->
	<scm>
		<connection>scm:git:https://github.com/shrinivas93/REST-in-peace.git</connection>
		<developerConnection>scm:git:https://github.com/shrinivas93/REST-in-peace.git</developerConnection>
		<url>https://github.com/shrinivas93/REST-in-peace</url>
		<tag>HEAD</tag>
	</scm>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>17</maven.compiler.source>
		<maven.compiler.target>17</maven.compiler.target>
		<!-- Overridden via -Drest-in-peace.version=... in CI to whatever
		     version was just installed locally from the current commit,
		     since this project's own default here drifts the moment the
		     core library's pom.xml version changes - see
		     .github/workflows/spring-boot-starter-test.yml. -->
		<rest-in-peace.version>1.0.0.1-SNAPSHOT</rest-in-peace.version>
		<spring-boot.version>4.1.1</spring-boot.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId>
				<version>${spring-boot.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>com.shri</groupId>
			<artifactId>rest-in-peace</artifactId>
			<version>${rest-in-peace.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<version>3.3.1</version>
				<configuration>
					<!-- Distinct prefix from core's own "v@{project.version}" tags -
					     both projects release from tags in this same repository, and
					     the two version numbers are completely independent (this
					     project started at 0.1.0, unrelated to core's 1.0.0.x line). -->
					<tagNameFormat>spring-boot-starter-v@{project.version}</tagNameFormat>
					<autoVersionSubmodules>true</autoVersionSubmodules>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<distributionManagement>
		<repository>
			<id>github</id>
			<name>GitHub Packages</name>
			<url>https://maven.pkg.github.com/shrinivas93/REST-in-peace</url>
		</repository>
	</distributionManagement>

</project>
