<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.shri</groupId>
		<artifactId>rest-in-peace-parent</artifactId>
		<version>1.0.0.37-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>

	<artifactId>rest-in-peace-spring-boot-starter</artifactId>
	<packaging>jar</packaging>

	<name>rest-in-peace-spring-boot-starter</name>
	<description>
		Spring Boot auto-configuration for REST-in-peace: auto-registers every
		@RestClient interface found on the classpath as a Spring bean, instead
		of one hand-written @Bean method per interface. See
		docs/design/spring-boot-starter.md in the parent repository for the
		full design and rollout plan. A sibling reactor module of core/ (not
		a dependent of it in the reactor sense - it resolves core the same
		way a real downstream consumer would, as an ordinary Maven
		dependency), sharing core's version and release cadence via their
		common parent. Requires Java 17+ and Spring Boot 4.x; the core
		rest-in-peace library itself stays on Java 8.
	</description>

	<!-- Version, scm, distributionManagement, and release-plugin config are
	     all inherited from the parent - see pom.xml's own comment for why. -->

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>17</maven.compiler.source>
		<maven.compiler.target>17</maven.compiler.target>
		<spring-boot.version>4.1.1</spring-boot.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId>
				<version>${spring-boot.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<!-- Same version as this module by construction - both inherit it
			     from the shared parent, so ${project.version} always names
			     whatever core version this build is paired with. -->
			<groupId>com.shri</groupId>
			<artifactId>rest-in-peace</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-autoconfigure</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.12.0</version>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
